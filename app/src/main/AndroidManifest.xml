<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.birthday_card2o">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Birthday_card2O">
        <activity
            android:name=".LayoutSix"
            android:exported="true" />
        <activity
            android:name=".LayoutFive"
            android:exported="true" />
        <activity
            android:name=".LayoutFour"
            android:exported="true" />
        <activity
            android:name=".LayoutThree"
            android:exported="true" />
        <activity
            android:name=".LayoutTwo"
            android:exported="true" />
        <activity
            android:name=".LayoutOne"
            android:exported="true" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" /> // provider_paths.xml file path in this example
        </provider>
        
    </application>

</manifest>